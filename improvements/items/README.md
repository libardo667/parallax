# Work Items

Create tracked work items in this tree:

- `majors/`
- `minors/`
- `patches/`
- `incidents/`
- `spikes/`

Use the local schema files in `improvements/` first, then fall back to harness
templates when a section or item type is missing.
